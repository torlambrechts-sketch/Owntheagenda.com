# OwnTheAgenda — environment template
# Copy to .env.local (gitignored) for local development.

# ---- Public client credentials (safe to expose; protected by RLS) ----
NEXT_PUBLIC_SUPABASE_URL=https://fqeohcfkimoopwjxxcft.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_h6rj3hux3D3oPct8FkfUJQ_cjkdtKO2

# ---- Server-only secret (NEVER expose to the browser, NEVER commit) ----
# Find it in: Supabase dashboard → Project Settings → API → service_role key.
SUPABASE_SERVICE_ROLE_KEY=

# Supabase project ref (for the CLI: `supabase link --project-ref ...`)
SUPABASE_PROJECT_REF=fqeohcfkimoopwjxxcft
